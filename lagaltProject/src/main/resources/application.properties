# Datasource configuration
spring.datasource.url= jdbc:postgresql://containers-us-west-201.railway.app:6567/railway
spring.datasource.username= postgres
spring.datasource.password= tIyd1ME2OFe7ktKR1yC1
# Hibernate behaviour
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL95Dialect
spring.jpa.hibernate.ddl-auto=create
#Turn Statistics on
logging.level.org.hibernate.stat= debug
# Enable logging of SQL
spring.jpa.show-sql=true
# Enable seeding. Doesn't use schema.sql but relies on Hibernate to create the schema.
spring.sql.init.platform= postgres
spring.jpa.defer-datasource-initialization= true
spring.sql.init.mode= always
